import express from "express";

//TODO: Check command syntax here.

export const CheckBrakeCommand = async (req, res, next) => {};

export const CheckAccelerateCommand = async (req, res, next) => {};

export const CheckPowerCommand = async (req, res, next) => {};

export const CheckDirectionCommand = async (req, res, next) => {};
